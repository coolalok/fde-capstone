[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers --tb=short
markers =
    slow: hits the real model provider; skipped by default (FR-24, A12)
    integration: cross-component tests
